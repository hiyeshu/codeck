{
  "name": "codeck",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:packages": "npm run build --workspace @codeck/core && npm run build --workspace @codeck/ai",
    "type-check:packages": "npm run type-check --workspace @codeck/core && npm run type-check --workspace @codeck/ai",
    "test": "vitest run"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^3.0.58",
    "@ai-sdk/openai": "^2.0.99",
    "ai": "^6.0.116",
    "pptxgenjs": "^4.0.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "playwright": "^1.58.2",
    "tsx": "^4.21.0",
    "typescript": "^5.7.3",
    "vitest": "^4.1.0"
  }
}
